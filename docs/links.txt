This is a file listing how everything links together and works.

What all the files do:
defs.h -
contains macro definitions and the global struct used for tracking state

init.c - 
contains:
init_screen()
destroy_screen()
↓
serves as the file to have the initialization and cleanup logic. The usr api imports it and makes a wrapper around it


internal_api.c-
contains:
add_change
add_
↓

usr_api.c-
contains:
↓



